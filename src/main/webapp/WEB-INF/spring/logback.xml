<?xml version="1.0" encoding="UTF-8"?>

<configuration>

	<!-- Appenders -->
	<appender name="util" class="ch.qos.logback.core.FileAppender">
		<File>${vikasworld.root}/WEB-INF/logs/vikasworld.log</File>
		<Append>true</Append>
		<!-- encoders are assigned the type ch.qos.logback.classic.encoder.PatternLayoutEncoder 
			by default -->
		<encoder>
			<pattern>%d{HH:mm:ss.SSS} %t %-5p %c{2} - %m%n</pattern>
		</encoder>
	</appender>

	<!-- Spring Logger -->
	<logger name="org.springframework" level="info" />

	<appender name="hibernate-sql" class="ch.qos.logback.core.FileAppender">
		<file>${vikasworld.root}/WEB-INF/logs/hibernate-sql.log</file>

		<layout class="ch.qos.logback.classic.PatternLayout">
			<pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n
			</pattern>
		</layout>
	</appender>

	<logger name="org.hibernate.SQL">
		<appender-ref ref="hibernate-sql" />
	</logger>

	<!-- Root Logger -->
	<!-- the level of the root level is set to DEBUG by default. -->
	<root level="debug">
		<appender-ref ref="util" />
	</root>

</configuration>